name: Publish a release

on:
  # release:
  #   types: [created]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo  waiting
    
  release:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: cdddd
    steps:
      - name: Download package
      - run: |
          echo approved
