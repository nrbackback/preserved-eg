name: Build Workflow
on:
  push:
    branches:
      - main
jobs:
  build:
   runs-on: ubuntu-latest
   steps:
    - name: Checkout repository
      uses: actions/checkout@v2
    - name: Read file content
      id: read-qa-file
      run: |
        FILE_CONTENT=$(cat qa.txt)
        echo "$FILE_CONTENT"
         if [ "$FILE_CONTENT" == "true" ]; then
           echo "build-qa=true" >> $GITHUB_ENV
         else
           echo "build-qa=false" >> $GITHUB_ENV
        fi
    - name: release_qa
      if: ${{ env.build-qa == 'true' }}
      run: |
        echo Add other actions to build,
        echo test, and deploy your project. 1
        echo steps.read-qa-file.outputs.content
        pwd
        go build
        go run .
    - name: release_qa
      if: ${{ env.build-qa == 'false' }}
      run: |
        echo false..................
